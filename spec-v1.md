# 教學 Panel — 規格文件 v1

> 實作課程的**即時進度看板**：講師帶學員動手做的時候，即時掌握每個人完成到哪、卡在哪。

---

## 1. 專案概觀

| 項目 | 內容 |
|------|------|
| 目的 | 課堂實作時，即時確認全班進度與卡點 |
| 使用時機 | 上課當下即時使用（2~3 小時實作課） |
| 規模 | 一堂課約 10~50 人 |
| 課後 | 進度資料可保留 / 匯出 |
| 平台 | 先做電腦版；手機版為 optional |

**核心結構決定：以「一堂課」為一個單位**，不拆成「課程／場次」兩層。
若同樣內容要開兩班，透過 **duplicate 整堂課** 來處理（複製關卡設計，不含學員與進度；複製後各自獨立）。

---

## 2. 角色與權限

| 角色 | 核心職責 | 主要畫面 |
|------|----------|----------|
| **學員 (Student)** | 回報自己各關卡的完成 / 卡住，並查看處理狀態 | 學員端 |
| **TA** | 救火：看誰卡住、認領、標記解決 | TA 畫面 |
| **講師 (Instructor)** | 掌握全局、控制節奏（簡易倒數）、隨時改題 | 講師控制台 ＋ 講師設計頁 |
| **Public（投影用）** | 全班一起看的進度節奏（不顯示名字） | Public 投影畫面 |
| **Admin** | 從平台入口管理所有課程 | Admin 頁面 |

> 權限關係：Admin ⊇ 講師 ⊇ TA。

### v1 的輕量權限方式

v1 **不建立正式帳號系統**，改用「持有哪一種角色專用連結，就能進入哪一種畫面」的簡單方式：

| 角色 | 進入方式 | 可以做什麼 |
|------|----------|------------|
| **學員** | 從課程的學員加入連結進入，自填暱稱；以該瀏覽器的輕量 session 辨識自己 | 查看關卡，只能更新自己的完成／卡住狀態與卡住原因 |
| **TA** | 從該堂課的 TA 專用連結進入，自填 TA 顯示名稱 | 查看卡住名單與課堂進度、認領／求救／解決問題；不能修改課程與關卡 |
| **講師** | 從該堂課的講師專用連結進入 | 使用控制台與設計頁、查看全班資料，並可執行 TA 的操作 |
| **Public** | 從投影專用連結進入 | 只能查看匿名的全班統計與簡易倒數，不能修改資料 |
| **Admin** | 從平台內部的 Admin 專用入口進入 | 查看與管理全部課程，並可進入各堂課的講師／TA 畫面 |

> 這是以連結本身作為權限憑證的輕量做法：不驗證真實身分、不建立講師帳號，也不實作「講師只能管理自己的課」。建立者在 v1 只是課程上的文字紀錄。專用連結若被轉傳，取得連結的人也會得到相同權限；正式帳號、課程所有權與更嚴格的授權移至 v2+。

---

## 3. 關卡 (Stage) 設計

- 講師事先設計一組關卡，每關有**標題**與**達成條件**（純文字說明，例如「終端機執行 `git --version` 看到版本號」）。
- 系統**不自動驗證**，由學員自己對照達成條件後手動回報。
- **關卡之間無順序性**：學員可任意回報任何一關（可能第 1 關完成、第 2 關卡住、第 3 關完成並存）。
- 關卡可**分區 / 章節**（呼應學員端、設計頁的分區呈現）。
- 講師**上課過程中可即時新增 / 修改**關卡（但需按「發布」才套用到學員端，見 §7.5）。
- 可用 **JSON 上傳** 一次匯入整組關卡。

### JSON 格式（v1 最小版本）

```json
{
  "courseTitle": "Git 入門實作",
  "stages": [
    {
      "id": "s1",
      "title": "安裝 Git",
      "criteria": "終端機執行 git --version 看到版本號"
    }
  ]
}
```

> 之後可再擴充欄位（連結、提示 hint、群組／章節等），見 §9 v2+。

---

## 4. 學員狀態與「卡住」處理流程

### 4.1 每關卡的學員狀態

每個學員 × 每個關卡，會有一個狀態：

| 狀態 | 說明 |
|------|------|
| 未開始 | 預設 |
| 已完成 | 學員自己按，可改回未完成 |
| 卡住 | 學員自己按，可填寫卡住原因（文字），並進入下方的 TA 認領處理流程 |

> 關卡可任意回報，同一個學員在不同關卡會有不同狀態並存。

### 4.2 認領機制（多 TA 協調）

每一筆卡住記錄有一個**認領狀態**，避免多位 TA 重工或漏接：

| 認領狀態 | 意義 |
|----------|------|
| **未處理** | 還沒有 TA 接手 |
| **處理中 by 某 TA** | 某 TA 已認領（按「我來處理」） |
| **求救 by 某 TA** | 該 TA 搞不定，按「求救」把它**丟回池子**；其他 TA／講師可接手。等同回到待認領，但帶有來歷（誰丟出來的），優先級視為高 |
| **已解決** | 處理完畢（按「已解決」） |

> 「求救」與「未處理」都屬於「需要有人接手」，在 TA／講師畫面應排在顯眼處。

---

## 5. 即時更新

- 各畫面（學員 / TA / 講師控制台 / public）採**每幾秒自動刷新一次**即可，不需要嚴格的 live 即時推播。
- 此前提讓學員狀態與 TA 認領狀態都能用較低的成本更新。

---

## 6. 匯出

- **基本**：匯出一張表
  - 列 = 學員
  - 欄 = 關卡
  - 格子 = 該學員該關卡的**最終狀態**
- 匯出入口放在 **Admin 課程列表**（每堂課的操作裡）。

---

## 7. 畫面規格（六大畫面）

### 7.1 學員端

學員整堂課盯著看的畫面。

**A. 頂部資訊**
- 課程標題
- 自己的暱稱（「你是 OOO」）
- 自己的進度（完成 X / 共 N 關，進度條）

**B. 關卡列表（主體，可分區呈現）**
每張關卡卡片：
- 關卡標題
- 達成條件說明（純文字）
- 自己在這關的狀態（未開始 / 完成 / 卡住）
- 「我完成了」按鈕（可改回未完成）
- 「我卡住了」按鈕
- 卡住時展開：填寫原因的輸入框 ＋ 目前**處理狀態**（未處理 / 處理中 / 求救 / 已解決）
- 〔進階〕附截圖

**C. 設計取捨（已定）**
- **看不到**別人 / 全班在各關的人數狀態（避免壓力）
- 關卡**可分區**

---

### 7.2 Public 投影畫面

投影到大螢幕、全班一起看，**不顯示名字**，只看節奏。

**A. 頂部**
- 課程標題
- 全班總人數（例如「共 32 人」）
- **簡易倒數**（顯示講師設定的倒數狀態，透過自動刷新取得更新；不保證不同裝置精準同步）

**B. 主體：每關一條長條**
- 每一關一列，長條顯示完成人數（例如「關卡 3：████████░░ 24/32」）
- **卡住人數**用紅色標出

**C. v1 不做**
- 卡關熱點自動提示

---

### 7.3 TA 畫面

TA 上課時救火用的畫面，核心是**看到誰卡住、誰還沒人接**。

**A. 卡住的人清單（主視圖）**
每一筆顯示：
- 學員暱稱
- 卡在哪一關
- 卡住原因（學員寫的文字）
- 〔進階〕截圖
- **認領狀態**：未處理 / 處理中 by 某 TA / 求救 by 某 TA / 已解決
- 操作按鈕：「我來處理」（認領）、「已解決」、「求救」（丟回池子讓別人接）

**B. 排序 / 篩選**
- 篩選：只看未處理 / 只看我認領的 / 看全部
- 預設排序依「卡在的關卡」；可切換成「最新 / 最舊問題」排序
- 已處理的**黯淡顯示**，凸顯未處理 / 求救的

**C. 全班進度總表**
- 預設**看不到**，可切 filter 打開
- 用途：撈出「卡住但沒按求助」的學員

---

### 7.4 講師控制台（上課時的主畫面）

講師一頁掌握全局 ＋ 控制節奏。可與「講師設計頁」快速切換。

**A. 頂部控制列**
- 課程標題
- **簡易倒數**：可設定分鐘數、開始與重設；public 透過自動刷新取得倒數狀態，允許不同裝置有數秒誤差；歸零時顯示「時間到」
- 一鍵切到「講師設計頁」
- 目前在線 / 已加入人數

**B-1. 全班進度總覽**
- 每關一條：完成人數 / 卡住人數
- 點某一關，可列出該關卡住的**名字**

**B-2. 求救 / 卡住即時清單**
- 預設只顯示「求救」和「未處理」的人，讓講師知道 TA 忙不忙、有沒有人喊救命

---

### 7.5 講師設計頁面（備課 / 上課中臨時改題）

建立與編輯整堂課的關卡。

**A. 課程基本資訊**
- 課程標題
- 〔可選〕課程說明

**B. 關卡編輯（主體）**
每一關可編輯：標題、達成條件（純文字）、所屬區塊 / 章節
操作：新增、刪除、**拖曳排序**、複製

**C. JSON 上傳 / 匯出**
- 上傳 JSON 一次匯入整組關卡
- 匯出目前關卡成 JSON（備份 / 重用 / duplicate）

**D. 上課中修改的處理（已定）**
- **刪除關卡 → 軟刪除**：資料與已完成紀錄保留，畫面隱藏
- **修改達成條件文字 → 不重置**已完成的人（「通知 / 重置」放進階再處理）

**E. 存檔機制（已定）**
- 編輯需按 **「發布 / 儲存」才套用到學員端**，避免改到一半就被學員看到半成品

---

### 7.6 Admin 頁面（v1：輕量課程管理中心）

定位：平台層級總覽，看得到系統內的**所有課程**，並作為 duplicate / 匯出 / 刪除 / 開新課的入口。v1 不依登入帳號區分課程所有權。

**A. 課程列表（主體）**
每堂課顯示：
- 課程標題
- 建立者（文字紀錄，不作為登入或授權依據）
- 建立時間 / 上課日期
- 狀態（進行中 / 已結束 / 草稿）
- 學員人數
- 操作：**duplicate**、**匯出**、**刪除（軟刪除）**、進入觀看

**B. 〔可選〕簡單統計**
- 平台總課數、累積學員數（概覽用，v1 不做也行）

**C. 權限**
- 持有 Admin 專用入口者可查看及管理**全部課程**
- 持有某堂課講師專用連結者，只能管理該連結對應的課程

> 「講師帳號與課程所有權」、「講師只能管理自己的課」以及「管理所有帳號」（停用帳號、改權限、看跨課程歷史）需等正式帳號系統存在，明確列為 **v2+**。

---

## 8. v1 範圍總結

**結構 / 進入**
- 一堂課為單位，可 duplicate（複製關卡設計）
- 以角色專用連結提供輕量權限，不建立正式帳號系統
- 輕量 Admin 課程列表（看所有課，duplicate / 匯出 / 刪除 / 開新課）

**學員端**
- 輕量加入（自己打暱稱，不需登入）
- 回報完成 / 卡住，狀態可改回
- 卡住可寫原因
- 看得到自己卡住的處理狀態
- 看不到別人狀態；關卡可分區

**TA 畫面**
- 卡住的人清單（名字、卡在哪關、原因）
- **認領機制**：未處理 / 處理中 by 某 TA / 求救 by 某 TA / 已解決
- 篩選（未處理 / 我認領的 / 全部）＋ 排序（卡關 / 最新 / 最舊）
- 可切換的全班進度總表（預設隱藏）

**講師**
- 講師控制台：簡易倒數（允許跨裝置有數秒誤差）＋ 全班進度總覽 ＋ 求救/卡住清單
- 講師設計頁：關卡 CRUD ＋ 拖曳排序 ＋ 分區 ＋ JSON 上傳/匯出 ＋ 軟刪除 ＋ 發布才生效
- 控制台 ↔ 設計頁快速切換

**Public 投影**
- 課程標題 ＋ 總人數 ＋ 簡易倒數
- 每關一條長條（完成人數 ＋ 卡住人數紅色標示）

**共通**
- 每幾秒自動刷新
- 匯出最終狀態表

---

## 9. v2+ 功能規劃

**帳號系統**
- v1 為輕量加入（暱稱、不登入）。
- 進階做**長期帳號**：Google Auth / Magic Link / 帳號密碼＋信箱認證（方式待定）。
- 以帳號落實講師／TA／Admin 的真實身分、課程所有權與權限隔離，讓講師只能管理自己的課。
- **管理帳號的 Admin**（停用帳號、改權限、看跨課程歷史）與帳號系統一起做。

**講師控制台增強**
- 完整的**全班進度矩陣**：學員 × 關卡的大表，每格顯示狀態，並處理分頁、展開與大量欄位的瀏覽體驗。
- 完整同步的**計時器**：支援倒數／正數、暫停／繼續／重設，以伺服器時間為準同步講師與 public，並可靠顯示「時間到」。

**卡住回報增強**
- 學員與 TA 可在每筆卡住記錄中使用**訊息清單**不限次數來回對話。
- 訊息靠每幾秒自動刷新更新，不需嚴格即時推播。
- 學員回報卡住可**附截圖**；TA 可幫忙核對截圖 / 達成條件。

**改題的連動處理**
- 修改達成條件文字時，**通知 / 重置**已完成的人。

**關卡欄位擴充**
- 每關可加：說明**連結**（下載點、文件）、**提示 hint** 等。

**進度歷程 / 分析**
- 每 **5 分鐘做一次 snapshot**，保留進度時間軸。
- 分析「某關大家平均卡多久」等節奏資料，回頭優化教材。

**Public 增強**
- 卡關熱點自動提示（某關卡住比例過高時自動變紅 / 跳提示）。

**平台**
- 手機版（學員可能用手機操作）。

---

## 10. 待設計 / 開放問題 (TODO)

- [ ] v1 各角色專用連結的產生、重設與失效方式
- [ ] 長期帳號系統的登入方式（v2+）
- [ ] 技術棧選型（目前無偏好）
